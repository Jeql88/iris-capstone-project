<UserControl x:Class="IRIS.UI.Views.Common.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="25,20" MaxWidth="700">
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                <ui:SymbolIcon Symbol="Settings24" FontSize="32" Foreground="{DynamicResource IRISPrimaryBrush}" Margin="0,0,12,0"/>
                <TextBlock Text="Settings" FontSize="32" FontWeight="Bold" 
                           Foreground="{DynamicResource IRISPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- User Profile Card -->
            <ui:Card Margin="0,0,0,20">
                <StackPanel Margin="15">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <ui:SymbolIcon Symbol="Person24" Foreground="{DynamicResource IRISSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="User Profile" FontSize="18" FontWeight="Bold" 
                                   Foreground="{DynamicResource IRISSecondaryBrush}"/>
                    </StackPanel>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Username:" FontSize="14" FontWeight="SemiBold" 
                                   Margin="0,0,0,15" Foreground="#1a1a1a" VerticalAlignment="Center"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Username, FallbackValue='—'}" FontSize="14" 
                                   Foreground="#333333" Margin="0,0,0,15" VerticalAlignment="Center" FontWeight="Medium"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Full Name:" FontSize="14" FontWeight="SemiBold" 
                                   Margin="0,0,0,15" Foreground="#1a1a1a" VerticalAlignment="Center"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding FullName, FallbackValue='—'}" FontSize="14" 
                                   Foreground="#333333" Margin="0,0,0,15" VerticalAlignment="Center" FontWeight="Medium"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Role:" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="#1a1a1a" VerticalAlignment="Center"/>
                        <Border Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left"
                                Background="{DynamicResource IRISPrimaryBrush}" CornerRadius="4" Padding="10,4">
                            <TextBlock Text="{Binding Role, FallbackValue='—'}" FontSize="13" 
                                       Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                    </Grid>
                </StackPanel>
            </ui:Card>

            <!-- Change Password Card -->
            <ui:Card Margin="0,0,0,20">
                <StackPanel Margin="15">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <ui:SymbolIcon Symbol="Key24" Foreground="{DynamicResource IRISSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="Change Password" FontSize="18" FontWeight="Bold" 
                                   Foreground="{DynamicResource IRISSecondaryBrush}"/>
                    </StackPanel>
                    
                    <TextBlock Text="Current Password" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#1a1a1a"/>
                    <ui:PasswordBox x:Name="CurrentPasswordBox" 
                                    PlaceholderText="Enter current password"
                                    Icon="{ui:SymbolIcon LockClosed24}"
                                    Margin="0,0,0,15"/>
                    
                    <TextBlock Text="New Password" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#1a1a1a"/>
                    <ui:PasswordBox x:Name="NewPasswordBox" 
                                    PlaceholderText="Enter new password"
                                    Icon="{ui:SymbolIcon Key24}"
                                    Margin="0,0,0,15"/>
                    
                    <TextBlock Text="Confirm New Password" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#1a1a1a"/>
                    <ui:PasswordBox x:Name="ConfirmPasswordBox" 
                                    PlaceholderText="Confirm new password"
                                    Icon="{ui:SymbolIcon Checkmark24}"
                                    Margin="0,0,0,20"/>
                    
                    <ui:Button x:Name="ChangePasswordButton" 
                               Content="Change Password" 
                               Icon="{ui:SymbolIcon ArrowRight24}"
                               Appearance="Primary"
                               HorizontalAlignment="Stretch"
                               Click="ChangePassword_Click"/>
                </StackPanel>
            </ui:Card>

            <!-- Session Card -->
            <ui:Card>
                <StackPanel Margin="15">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <ui:SymbolIcon Symbol="SignOut24" Foreground="{DynamicResource IRISSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="Session" FontSize="18" FontWeight="Bold" 
                                   Foreground="{DynamicResource IRISSecondaryBrush}"/>
                    </StackPanel>
                    
                    <ui:Button x:Name="LogoutButton" 
                               Content="Logout" 
                               Icon="{ui:SymbolIcon SignOut24}"
                               Appearance="Danger"
                               HorizontalAlignment="Stretch"
                               Click="Logout_Click"/>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
