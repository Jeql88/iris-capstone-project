<UserControl x:Class="IRIS.UI.Views.Faculty.ViewScreenPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:IRIS.UI.ViewModels"
             xmlns:converters="clr-namespace:IRIS.UI.Converters">
    
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="25,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <ui:SymbolIcon Symbol="Eye24" FontSize="32" Foreground="{DynamicResource IRISPrimaryBrush}" Margin="0,0,12,0"/>
                    <TextBlock Text="View Screen" FontSize="32" FontWeight="Bold" 
                               Foreground="{DynamicResource IRISPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <ui:Button Command="{Binding BackCommand}" 
                           Appearance="Secondary" BorderThickness="0" Padding="0"
                           Background="Transparent" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ArrowLeft24" Foreground="{DynamicResource IRISSecondaryBrush}" Margin="0,0,6,0"/>
                        <TextBlock Text="Back to Tiled-View" FontSize="14" 
                                   Foreground="{DynamicResource IRISSecondaryBrush}" FontWeight="SemiBold"/>
                    </StackPanel>
                </ui:Button>
            </StackPanel>

            <!-- Connection Status -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{DynamicResource OnlineStatusBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                    <Setter Property="Fill" Value="{DynamicResource OfflineStatusBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding ConnectionStatus}" FontSize="14" 
                           Foreground="#666666"/>
            </StackPanel>
        </Grid>

        <!-- Screen Display Area -->
        <ui:Card Grid.Row="1" Margin="0,0,0,20">
            <Border CornerRadius="8" ClipToBounds="True">
                <Grid>
                    <!-- Screen Image -->
                    <Image x:Name="ScreenImage" 
                           Source="{Binding ScreenImage}" 
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    
                    <!-- Loading/Disconnected Overlay -->
                    <Border Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ui:ProgressRing IsIndeterminate="True" Width="60" Height="60"/>
                            <TextBlock Text="Connecting to PC..." FontSize="16" Margin="0,20,0,0" Foreground="#1a1a1a"/>
                        </StackPanel>
                    </Border>

                    <!-- Disconnected State -->
                    <Border Visibility="{Binding IsDisconnected, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Border Width="80" Height="80" CornerRadius="40" 
                                    Background="{DynamicResource OfflineStatusBrush}" Margin="0,0,0,20">
                                <ui:SymbolIcon Symbol="Desktop24" FontSize="36" Foreground="White"/>
                            </Border>
                            <TextBlock Text="PC is Offline" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#1a1a1a"/>
                            <TextBlock Text="The selected PC is not currently connected" 
                                       FontSize="14" Foreground="#666666"/>
                            <ui:Button Content="Retry Connection" 
                                       Icon="{ui:SymbolIcon ArrowClockwise24}"
                                       Command="{Binding RetryConnectionCommand}"
                                       Appearance="Danger" Margin="0,20,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </ui:Card>

        <!-- PC Info and Controls Panel -->
        <ui:Card Grid.Row="2">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- PC Name and Controls Row -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- PC Info -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Border Width="50" Height="50" CornerRadius="8" 
                                Background="{DynamicResource IRISPrimaryBrush}" Margin="0,0,15,0">
                            <ui:SymbolIcon Symbol="Desktop24" FontSize="24" Foreground="White"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding PCName}" FontSize="20" FontWeight="Bold" Margin="0,0,10,0" Foreground="#1a1a1a"/>
                                <Border Background="{DynamicResource IRISPrimaryBrush}" CornerRadius="4" Padding="8,2">
                                    <TextBlock Text="{Binding PCNumber}" FontSize="11" Foreground="White"/>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="{Binding RoomName}" FontSize="13" 
                                       Foreground="#666666"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Control Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <ui:Button Icon="{ui:SymbolIcon ArrowClockwise24}" 
                                   ToolTip="Refresh"
                                   Command="{Binding RefreshScreenCommand}"
                                   Appearance="Secondary" Margin="0,0,8,0"/>
                        <ui:Button Icon="{ui:SymbolIcon Power24}" 
                                   Content="Shutdown"
                                   Command="{Binding ShutdownPCCommand}"
                                   Appearance="Danger" Margin="0,0,8,0"/>
                        <ui:Button Icon="{ui:SymbolIcon ArrowClockwise24}" 
                                   Content="Restart"
                                   Command="{Binding RestartPCCommand}"
                                   Appearance="Secondary"/>
                    </StackPanel>
                </Grid>

                <!-- Expandable Details Section -->
                <StackPanel Grid.Row="1" Margin="0,15,0,0">
                    <!-- Toggle Details Button -->
                    <ui:Button Command="{Binding ToggleDetailsCommand}" 
                               Appearance="Secondary" BorderThickness="0"
                               Background="Transparent" HorizontalAlignment="Left" Padding="0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ExpandIcon}" 
                                       Foreground="{DynamicResource IRISSecondaryBrush}" Margin="0,0,6,0"
                                       VerticalAlignment="Center" FontSize="12"/>
                            <TextBlock Text="View PC Details" FontSize="14" 
                                       Foreground="{DynamicResource IRISSecondaryBrush}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </ui:Button>

                    <!-- Details Panel -->
                    <Border Margin="0,15,0,0" 
                            Visibility="{Binding IsDetailsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Network Info -->
                                <ui:Card Grid.Column="0">
                                    <StackPanel Margin="12">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <ui:SymbolIcon Symbol="Globe24" Foreground="{DynamicResource IRISPrimaryBrush}" Margin="0,0,8,0"/>
                                            <TextBlock Text="Network Information" FontWeight="Bold" FontSize="14" 
                                                       Foreground="{DynamicResource IRISPrimaryBrush}"/>
                                        </StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="IP Address:" FontSize="13" 
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding IP}" FontSize="13" Margin="0,0,0,8" Foreground="#1a1a1a"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Subnet Mask:" FontSize="13" 
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Subnet}" FontSize="13" Margin="0,0,0,8" Foreground="#1a1a1a"/>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Network:" FontSize="13" 
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Network}" FontSize="13" Margin="0,0,0,8" Foreground="#1a1a1a"/>

                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="MAC Address:" FontSize="13" 
                                                       Foreground="#666666"/>
                                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding MacAddress}" FontSize="13" Foreground="#1a1a1a"/>
                                        </Grid>
                                    </StackPanel>
                                </ui:Card>

                                <!-- System Info -->
                                <ui:Card Grid.Column="2">
                                    <StackPanel Margin="12">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <ui:SymbolIcon Symbol="Info24" Foreground="{DynamicResource IRISPrimaryBrush}" Margin="0,0,8,0"/>
                                            <TextBlock Text="System Information" FontWeight="Bold" FontSize="14" 
                                                       Foreground="{DynamicResource IRISPrimaryBrush}"/>
                                        </StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="40"/>
                                                <ColumnDefinition Width="95"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="OS:" FontSize="13" 
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding OS}" FontSize="13" Margin="0,0,0,8" Foreground="#1a1a1a"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="CPU Usage:" FontSize="13" 
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                                                <ProgressBar Value="{Binding CpuUsage}" Width="80" Height="8" 
                                                            Foreground="{DynamicResource IRISPrimaryBrush}"/>
                                                <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F0}%}" FontSize="12" Margin="8,0,0,0" Foreground="#1a1a1a"/>
                                            </StackPanel>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="RAM Usage:" FontSize="13" 
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                                                <ProgressBar Value="{Binding RamUsage}" Width="80" Height="8" 
                                                            Foreground="{DynamicResource IRISSecondaryBrush}"/>
                                                <TextBlock Text="{Binding RamUsage, StringFormat={}{0:F0}%}" FontSize="12" Margin="8,0,0,0" Foreground="#1a1a1a"/>
                                            </StackPanel>

                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Disk Usage:" FontSize="13" 
                                                       Foreground="#666666"/>
                                            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                                                <ProgressBar Value="{Binding DiskUsage}" Width="80" Height="8" 
                                                            Foreground="{DynamicResource IRISAccentBrush}"/>
                                                <TextBlock Text="{Binding DiskUsage, StringFormat={}{0:F0}%}" FontSize="12" Margin="8,0,0,0" Foreground="#1a1a1a"/>
                                            </StackPanel>

                                            <TextBlock Grid.Row="1" Grid.Column="3" Text="CPU Temp:" FontSize="13"
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="1" Grid.Column="4" Text="{Binding CPUTemp}" FontSize="12"
                                                       Foreground="#1a1a1a" Margin="0,0,0,8"/>

                                            <TextBlock Grid.Row="2" Grid.Column="3" Text="GPU Temp:" FontSize="13"
                                                       Foreground="#666666" Margin="0,0,0,8"/>
                                            <TextBlock Grid.Row="2" Grid.Column="4" Text="{Binding GPUTemp}" FontSize="12"
                                                       Foreground="#1a1a1a" Margin="0,0,0,8"/>
                                        </Grid>
                                    </StackPanel>
                                </ui:Card>
                            </Grid>

                            <!-- Hardware Specifications -->
                            <ui:Card Margin="0,15,0,0">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <ui:SymbolIcon Symbol="Desktop24" Foreground="{DynamicResource IRISPrimaryBrush}" Margin="0,0,8,0"/>
                                        <TextBlock Text="Hardware Specifications" FontWeight="Bold" FontSize="14" 
                                                   Foreground="{DynamicResource IRISPrimaryBrush}"/>
                                    </StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Left Column -->
                                        <StackPanel Grid.Column="0">
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Processor:" FontSize="13" Foreground="#666666"/>
                                                <TextBlock Grid.Column="1" Text="{Binding CPUModel}" FontSize="13" Foreground="#1a1a1a" TextWrapping="Wrap"/>
                                            </Grid>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Graphics:" FontSize="13" Foreground="#666666"/>
                                                <TextBlock Grid.Column="1" Text="{Binding GPUModel}" FontSize="13" Foreground="#1a1a1a" TextWrapping="Wrap"/>
                                            </Grid>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Motherboard:" FontSize="13" Foreground="#666666"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Motherboard}" FontSize="13" Foreground="#1a1a1a" TextWrapping="Wrap"/>
                                            </Grid>
                                        </StackPanel>

                                        <!-- Right Column -->
                                        <StackPanel Grid.Column="2">
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="RAM:" FontSize="13" Foreground="#666666"/>
                                                <TextBlock Grid.Column="1" Text="{Binding RAMModel}" FontSize="13" Foreground="#1a1a1a" TextWrapping="Wrap"/>
                                            </Grid>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Storage:" FontSize="13" Foreground="#666666"/>
                                                <TextBlock Grid.Column="1" Text="{Binding StoragePrimary}" FontSize="13" Foreground="#1a1a1a" TextWrapping="Wrap"/>
                                            </Grid>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </ui:Card>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ui:Card>
    </Grid>
</UserControl>
