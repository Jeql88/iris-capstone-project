<ui:FluentWindow x:Class="IRIS.UI.Views.Shared.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:IRIS.UI.Converters"
        mc:Ignorable="d"
        Title="IRIS - Login" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>

        <ui:TitleBar Grid.ColumnSpan="2" Title="IRIS - Login" Foreground="White" Panel.ZIndex="1"/>

        <!-- Left Panel - Background Image -->
        <Border Grid.Column="0" CornerRadius="8,0,0,8">
            <Border.Background>
                <ImageBrush ImageSource="/IRIS.UI;component/assets/safad_login.png" Stretch="UniformToFill"/>
            </Border.Background>
        </Border>

        <!-- Right Panel - Login Form -->
        <Border Grid.Column="1" CornerRadius="0,8,8,0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#8B0000" Offset="0"/>
                    <GradientStop Color="#620F1E" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            
            <Grid Margin="50,80,50,50">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Logo/Title -->
                <StackPanel Grid.Row="0" Margin="0,0,0,40">
                    <TextBlock Text="IRIS" FontSize="48" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Integrated Resource Information System" FontSize="12" Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Username Label -->
                <TextBlock Grid.Row="1" Text="Username" FontSize="14" FontWeight="Medium" Foreground="White" Margin="0,0,0,8"/>

                <!-- Username Input -->
                <ui:TextBox Grid.Row="2" 
                            PlaceholderText="Enter your username"
                            Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                            Icon="{ui:SymbolIcon Person24}"
                            Margin="0,0,0,20"
                            KeyDown="OnKeyDown"/>

                <!-- Password Label -->
                <TextBlock Grid.Row="3" Text="Password" FontSize="14" FontWeight="Medium" Foreground="White" Margin="0,0,0,8"/>

                <!-- Password Input -->
                <ui:PasswordBox Grid.Row="4" 
                                x:Name="PasswordBox" 
                                PlaceholderText="Enter your password"
                                Icon="{ui:SymbolIcon LockClosed24}"
                                Margin="0,0,0,15"
                                KeyDown="OnKeyDown"/>

                <!-- Error Message -->
                <ui:InfoBar Grid.Row="5" 
                           Title="Login Error"
                           Message="{Binding ErrorMessage}"
                           Severity="Error"
                           IsOpen="{Binding HasError}"
                           IsClosable="False"
                           Margin="0,0,0,20"/>

                <!-- Login Button -->
                <ui:Button Grid.Row="6" 
                          Appearance="Primary"
                          Command="{Binding LoginCommand}" 
                          HorizontalAlignment="Stretch"
                          Height="45"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Icon="{ui:SymbolIcon ArrowRight24}">
                    <TextBlock Text="Sign In" Foreground="White"/>
                </ui:Button>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.ColumnSpan="2" 
                Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                CornerRadius="8">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="60" Height="60"/>
                <TextBlock Text="Signing in..." Foreground="White" FontSize="14" Margin="0,15,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
